import template from './timer.html';
import controller from './timer.controller';
import './timer.scss';

let timerComponent = {
  template,
  controllerAs: 'vm',
  controller
};

export default timerComponent;
